export enum tagTypes {
  donors = "donors",
  admin = "admin",
  recipient = "recipient",
  user = "user",
  requests = "requests",
}

export const tagTypesList = [
  tagTypes.donors,
  tagTypes.admin,
  tagTypes.recipient,
  tagTypes.user,
  tagTypes.requests,
];
